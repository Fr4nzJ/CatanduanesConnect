*.bin filter=lfs diff=lfs merge=lfs -text
*.safetensors filter=lfs diff=lfs merge=lfs -text
# Ensure the tokenizer json in the model folder is not handled by LFS (we want HF to re-download a compatible one)
model/tokenizer.json -filter -diff -merge -text
